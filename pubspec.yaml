name: lio_rtc
description: "Lio Live Flutter SDK — real-time voice, video, live streaming and audio rooms by AppLooma LLC."
version: 0.1.0
homepage: https://docs.liolive.com
repository: https://github.com/applooma/lio-rtc-flutter

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  livekit_client: ^2.3.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
